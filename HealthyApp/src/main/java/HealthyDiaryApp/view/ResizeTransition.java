package HealthyDiaryApp.view;

import javafx.animation.Transition;
import javafx.util.Duration;

import javax.swing.plaf.synth.Region;

/*
public class ResizeTransition extends Transition {
    private Region region;
    private double startHeight;
    private double endHeight;

    public ResizeTransition(Duration duration, Region region, double endHeight) {
        setCycleDuration(duration);
        this.region = region;
        this.startHeight = region.getHeight();
        this.endHeight = endHeight;
    }

    @Override
    protected void interpolate(double frac) {
        double newHeight = startHeight + frac * (endHeight - startHeight);
        region.setPrefHeight(newHeight);
    }
}
*/

